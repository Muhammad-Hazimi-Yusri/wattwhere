name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci

      - run: npm test

      - name: Populate GB power data if seed is empty
        run: |
          count=$(jq '.features | length' public/data/gb-power.geojson)
          if [ "$count" = "0" ]; then
            echo "seed file empty; fetching from OSM"
            npm run data:osm
          else
            echo "using committed gb-power.geojson ($count features)"
          fi

      - name: Populate GB DNO regions if seed is empty
        run: |
          count=$(jq '.features | length' public/data/gb-regions.geojson)
          if [ "$count" = "0" ]; then
            echo "regions seed empty; fetching from NESO"
            npm run data:regions
          else
            echo "using committed gb-regions.geojson ($count features)"
          fi

      - name: Verify data artifacts
        run: |
          regions=$(jq '.features | length' public/data/gb-regions.geojson)
          [ "$regions" -ge 14 ] || { echo "gb-regions.geojson has $regions features — populate failed"; exit 1; }
          power=$(jq '.features | length' public/data/gb-power.geojson)
          [ "$power" -gt 0 ] || { echo "gb-power.geojson has 0 features — populate failed"; exit 1; }
          echo "data ok: regions=${regions} power=${power}"

      - run: npm run build
        env:
          BASE_PATH: /wattwhere
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
